let number = document.getElementById("number");


function submitNum() {
    let theNumber = number.value
    let tip = theNumber * .20
    let total = (theNumber +++ tip)

    console.log("Tip:" + "$" + tip  + " " + "Total:" + "$" + total)
   
}
